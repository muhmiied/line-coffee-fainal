import { FlavorMixStudio } from "@/features/website/make-your-flavor/FlavorMixStudio";

export const metadata = {
  title: "Make Your Flavor | Line Coffee",
  description: "Build your custom flavor mix with our Flavor Mix Studio.",
};

export default function MakeYourFlavorPage() {
  return <FlavorMixStudio />;
}
